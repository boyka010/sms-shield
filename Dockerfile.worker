FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci --only=production

COPY . .
RUN npx prisma generate

ENV NODE_ENV=production

CMD ["tsx", "server/worker.ts", "all"]
